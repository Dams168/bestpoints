<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Bestpoint: <%= place.title %></title>
  </head>
  <body>
    <h1><%= place.title %></h1>
    <h2><%= place.location %></h2>

    <form action="/place/<%= place._id %>/delete?_method=DELETE" method="POST">
      <button type="submit">Delete</button>
    </form>
    <a href="/place/<%= place._id %>/edit">Edit</a>
    <a href="/places">All Place</a>
  </body>
</html>
