<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Bespoints - places</title>
  </head>
  <body>
    <h1>All Places</h1>
    <a href="/place/create">Create New Place</a>
    <ul>
      <% places.forEach((place) => { %>
      <li>
        <a href="/place/<%= place._id %>"><%= place.title %></a>
      </li>
      <% }); %>
    </ul>
  </body>
</html>
